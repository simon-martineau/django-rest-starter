language: python

python:
    - "3.9"

services:
    - docker

before_script: pip install docker-compose

script:
    - docker-compose -f docker-compose.dev.yml run api flake8
    - docker-compose -f docker-compose.dev.yml run api python3 manage.py test